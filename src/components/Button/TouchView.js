import React from 'react';
import { TouchableOpacity } from 'react-native';


const TouchView = (props) => <TouchableOpacity {...props} />;

TouchView.displayName = 'Button_TouchView';

export default TouchView;
