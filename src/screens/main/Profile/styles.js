import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainWrapper: {
    marginHorizontal: 16,
    marginTop: 74,
    flex: 1,
  },
  form: {
    marginBottom: 15,
  },
});

export default styles;